# 2026-07-30 迭代记录

## 基本信息

| 项目 | 内容 |
|------|------|
| 日期 | 2026-07-30 |
| 目标 | 参考 record：Supabase 保活健康检查；订单支付/加菜/取消 |
| 状态 | 已完成 |

## 本轮变更摘要

- 新增 `GET /api/health`：Bearer `HEALTH_CHECK_SECRET`，轻量查询 `profiles`。
- 新增 `.github/workflows/keep-alive.yml`：每月 1/21 日（UTC 02:00）或手动触发。
- `.env.example` 与部署 / 接口文档同步说明。
- 二级页顶栏标题居中（`PageHeader`）。
- 订单流：结算 → **待支付**；详情有加菜 / 去支付；去支付 → **已确认**；已确认可加菜 / 已完成；已确认加菜后回待支付，`payable_amount` 仅为加菜金额。迁移 `004_order_payable.sql`。
- 待支付可**取消**（无已付明细）；部分支付不可取消；未付加菜可**移除**。迁移 `005_order_item_paid.sql`。
- 应用名改为 **小菜单**；图标统一为 `logo.ico`（favicon / 苹果主屏幕 / PWA / 登录站标）；新增 `app/manifest.ts`。
- 品牌站标替换为笑脸餐盘 3D 图标（满幅橙色底）：同步更新 `app/logo.ico`、`app/icon.ico`、`public/logo.ico`；登录页站标改为完整展示图标（不再圆形裁切黄底）。
- 接入 **PWA Service Worker**（`@serwist/turbopack`）：生产注册 `/serwist/sw.js`，预缓存壳层与图标，导航缓存已访问页，离线回退 `/~offline`；补充 `public/icons/icon-{192,512}.png` 供安装提示。
- **数据统计**：我的 → `/stats`（订单汇总 + 菜品下单排行）；点菜页/详情展示「已点 N 次」（非取消订单份数累加）。
- **菜品多图**：`image_urls`（最多 9）；`image_url` 同步为首图封面；列表/详情 contain 完整显示，点击全屏预览。迁移 `006_dish_image_urls.sql`。
- **上传前压缩**：`browser-image-compression`（最长边 1920、约 1.2MB）；服务端上限改为 3.5MB，避免 `FUNCTION_PAYLOAD_TOO_LARGE`。
- **点菜记录搜索**：按菜品名 / 订单号片段搜索，可与状态筛选、分页组合。
- **首屏体验白屏**：品牌 `BootSplash`；菜单与已点次数解耦（次数客户端延后）；预缓存登录页与静态图标。

## 涉及文档

- [x] deployment / api / development-guide / README / .env.example / PRD / database

## 验收要点

- [ ] 部署环境配置 `HEALTH_CHECK_SECRET`
- [ ] GitHub Actions Secrets：`HEALTH_CHECK_URL`、`HEALTH_CHECK_SECRET`
- [ ] Actions 手动 Run workflow 返回 HTTP 200
- [ ] 执行 `004_order_payable.sql`、`005_order_item_paid.sql`、`006_dish_image_urls.sql`
- [ ] 结算后为待支付；去支付后已确认；加菜金额正确
- [ ] 未支付可取消；部分支付不可取消；未付菜可移除
- [ ] 浏览器标签与「添加到主屏幕」显示「小菜单」与新图标
- [ ] 我的可进数据统计；排行与汇总正确；点菜页显示已点次数
- [ ] 菜品可传多图；列表/详情完整显示并可预览
- [ ] 大图上传先压缩再传，线上不再出现 FUNCTION_PAYLOAD_TOO_LARGE
- [ ] 点菜记录可搜索菜品名并筛选状态
- [ ] 桌面 PWA / 首次进入出现品牌加载壳，而非长时间纯白屏
