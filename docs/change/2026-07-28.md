# 2026-07-28 迭代记录

## 基本信息

| 项目 | 内容 |
|------|------|
| 日期 | 2026-07-28 |
| 目标 | M0 文档 + M1/M2 功能落地（登录、点菜结算、订单、菜品管理、ImgBB） |
| 状态 | 开发中（待配置 Service Role 并执行迁移后联调） |

## 本轮变更摘要

- 完成文档体系与 Next.js 应用脚手架。
- 鉴权：`profiles.password_hash` + JWT Cookie（`menu_session`），非 Supabase Auth。
- 页面：登录、点菜（搜索/分类/购物车结算）、我的、订单列表/详情、菜品管理与表单上传。
- 迁移：`supabase/migrations/001_init.sql`（表结构 + 种子账号/菜品）。
- UI：暖黄品牌色，对齐 docs 设计图主流程；图标统一为 **lucide-react**。

## 涉及文档

- [x] PRD / database / api / cache / development / deployment / README

## 验收要点

- [x] `npm run build` 通过
- [ ] Supabase 执行迁移 + 配置 `SUPABASE_SERVICE_ROLE_KEY` 后全流程联调

## 补充（同日）

- 点菜页：左右分类滚动联动；购物车上移与底 Tab 留白；点击菜品打开详情弹层。
- 图标：改为 **lucide-react**。
- **每用户独立菜单**：`categories` / `dishes` 增加 `user_id`；去掉 admin 菜单门槛；缓存标签 `menu:{userId}`。
- **分类管理**：我的 → 分类管理；增删改本人分类（`app/actions/category.ts`）。

## 风险与后续

- 已有数据库需执行 `002_per_user_menu.sql`（会清空订单/菜品/分类后重灌种子）。
- Next 16 提示 middleware → proxy 迁移，可后续跟进。
